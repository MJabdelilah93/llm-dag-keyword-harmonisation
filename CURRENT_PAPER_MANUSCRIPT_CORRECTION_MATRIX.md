# Current-Paper Manuscript Correction Matrix

**This document does not edit the manuscript.** The manuscript itself is
not present anywhere in this workspace (confirmed absent in the 2026-08-23
audit; it is drafted externally in Overleaf) — "exact current claim" below
is reconstructed from the best available proxy: `INTERNAL_PROJECT_STATE.md`
(the locked internal notes), the repository's own `README.md`/`docs/*.md`
before this repair, and, where the Phase 0B task prompt itself quoted
manuscript language directly, that quote. Where no proxy exists, this is
stated explicitly rather than invented. A separate drafting pass (outside
this repair) should produce the final Replace:/With: text against the
actual manuscript file.

| # | Topic | Manuscript section (best guess) | Current claim | Evidence | Classification | Corrected factual content | Changes conclusion? |
|---|---|---|---|---|---|---|---|
| 1 | Normalisation | §2.2 Methodology | "Unicode NFKC normalisation, lowercasing, whitespace collapsing, punctuation standardisation, and common acronym cleanup are applied" (repository README, pre-repair) | `docs/provenance/v1_execution_config_provenance.md`; `tests/test_normalise.py` | METHOD DESCRIPTION CORRECTION | Only 4 steps ran: Unicode NFKC, lowercase, strip, whitespace collapse. No punctuation standardisation, no acronym expansion, at Node 2. | No — no reported number depends on the unimplemented steps |
| 2 | JSON/schema wording | §2.4 Methodology (Node 4) | "structured JSON output via schema in prompts/schemas/"; guard performs "schema validation" | `docs/provenance/v1_execution_config_provenance.md`; `docs/workflow_description.md` | METHOD DESCRIPTION CORRECTION | Output is requested via prompt instruction only; parsing is markdown-fence-strip + `json.loads()`. `schemas/llm_response.schema.json` documents the intended shape but is never programmatically validated against. | No |
| 3 | Guard confidence threshold | §2.4/§2.5 | Implies asymmetric thresholds (match/non-match) per `configs/guard_thresholds.yaml` | `results/tuned_thresholds.json`; independently re-verified twice (Phase 0A, Phase 0B Task 4) | REPRODUCIBILITY CORRECTION | Single symmetric threshold = 0.50, selected by a dev-set grid sweep [0.50, 0.95] maximising F1 subject to coverage ≥ 0.70. | No — the reported number (0.50) is what actually ran and is correct; only the *documented rationale* (asymmetric) was wrong |
| 4 | G5 contradiction check | §2.4 Methodology (Node 5) | Guard layer includes "contradiction check (intransitive triples) → flag, route to manual review" | `docs/provenance/v1_retrospective_run_manifest.json`; `docs/workflow_description.md` | CLAIM REMOVAL | G5 was designed but never implemented. No pair was ever flagged by it in v1. Remove the claim, or explicitly label it a planned-but-unimplemented safeguard. | No — it never affected any reported decision |
| 5 | Auxiliary title/abstract context | §2.4 Methodology (Node 4) | "bounded auxiliary context (up to 3 titles per keyword) is included" for difficult pairs | Exhaustive grep, zero hits; ablation A3 is a verbatim copy of the full-pipeline row | CLAIM REMOVAL | Auxiliary context was never supplied for any of the 500 benchmark pairs. Remove the claim or explicitly label it unused-in-v1. | No |
| 6 | Ablation A3 | §3 Results, ablation table | Presented as an independently-run "no context" ablation condition | `docs/provenance/v1_execution_config_provenance.md`; source: `scripts/run_ablations.py` | METHOD DESCRIPTION CORRECTION | A3's row is copied verbatim from the full-pipeline row because context was never added to begin with — it is not an independent experimental condition. State this explicitly, or remove A3 from the ablation table as uninformative. | Possibly — if A3 is currently cited as evidence that "removing context has no effect," that specific inference is unsupported (context was never present to remove) |
| 7 | Canonicalisation rule | §2.4 Methodology (Node 7) | 4-tier priority rule (controlled vocabulary &gt; frequency &gt; expanded/shortest form &gt; alphabetical) | `docs/provenance/canonical_label_rule_check.md` | METHOD DESCRIPTION CORRECTION | Only highest-raw-frequency ran. ~17–20% of multi-member cluster labels would differ under the documented rule; cluster *membership* (and therefore every quantitative result) is unaffected. | No |
| 8 | Run manifests / audit trail | §2.6 Reproducibility | Per-run manifest (`runs/<timestamp>/manifest.yaml`), guard-decision logs, candidate-pair traces, manual override log all described as exported | `docs/workflow_description.md`; `runs/` directory confirmed empty | REPRODUCIBILITY CORRECTION | None of these were ever produced. The only real audit trail is `results/llm_logs/*.jsonl` (raw calls) and the final mapping table. State this precisely rather than describing an aspirational audit-trail architecture as operational. | No — reported numbers were independently re-derived directly from the raw logs regardless |
| 9 | Deterministic-rerun claim | §2.6/§4 Discussion | "Deterministic stages: identical rerun on same snapshot" | `docs/provenance/downstream_determinism_repair.md` | REPRODUCIBILITY CORRECTION | True for Nodes 1–3/6–7 (confirmed exactly, Phase 0B). **Not originally true** for Node 8 (downstream Louvain): modularity/community-count/ARI/AMI varied between runs, including one ARI ranking reversal, traced to non-deterministic graph-construction ordering — now fixed and verified (20/20 hash seeds bit-identical). Narrow the claim to the nodes it actually covers, and disclose the fix. | Yes, for the ARI-based claim specifically — see #13 |
| 10 | 39 annotator-hours | §3.4 / Methodology | Stated as a specific measured total | No file in the workspace computes or logs this figure; `INTERNAL_PROJECT_STATE.md` itself lists it as needing confirmation | CLAIM NARROWING or REMOVAL | Classified UNSOURCED ESTIMATE (Phase 0A Task 7). Either replace with a hedged estimate, or remove the specific figure until the authors confirm its source. Separately, the annotation-file timestamps show the whole campaign compressed into an 11h41m single-day window with full-round sheets predating pilot completion — this needs author confirmation before any number is asserted (see the "Questions for the authors" list, Phase 0A Task 7). | Possibly, if the annotation-provenance questions are not resolved satisfactorily |
| 11 | 109-term claim | §3.3/§4.1 (downstream qualitative discussion) | "Under B3, the largest single connected-components merge group collapsed 109 raw keyword strings into one canonical node" | `docs/provenance/109_term_claim_investigation.md` | NUMERIC CORRECTION or CLAIM REMOVAL | CANNOT REPRODUCE under any code-grounded definition. Closest verified figure: 115 keywords with corpus frequency ≥ 5 in the "Sustainability" cluster (not 109); the unfiltered largest B3 cluster has 374 members. Replace with a verified figure or remove pending the authors locating the original computation. | No — this is an illustrative example, not a headline metric |
| 12 | "194" wording | §3.3 downstream results | "194 cluster pairs" split by LLM-DAG | `docs/provenance/downstream_determinism_repair.md` §6 in the earlier audit; reconfirmed exactly in Phase 0B (194) | METHOD DESCRIPTION CORRECTION (wording only) | The value 194 is correct and reproducible, but "cluster pairs" is imprecise — it counts B3 clusters (not pairs) that fragment into ≥2 LLM-DAG clusters, corpus-wide (not sustainability-specific, despite its position in the text next to the sustainability figures). Reword only; no numeric change. | No |
| 13 | Table 9 downstream metrics | Table 9 (per `results/llm_logs/README.md`'s current numbering) | Point estimates: Q=0.220/0.237, ARI=0.201/0.214, AMI=0.196/0.216 (B3/LLM-DAG) | `results/current_paper/downstream_results_corrected.csv`; `docs/provenance/downstream_determinism_repair.md` | NUMERIC CORRECTION + NEW RESULT NEEDED | Vocabulary/edges/density: unchanged, confirmed exact. Modularity Q: report as median [IQR] across Louvain seeds (B3 0.2175 [0.2161, 0.2185]; LLM-DAG 0.2361 [0.2348, 0.2372]) — ranking is robust (100% of 50 seeds). AMI: report as distribution (B3 0.2011 [0.1924, 0.2128]; LLM-DAG 0.2197 [0.2128, 0.2327]) — mostly robust (84% of seeds). **ARI: NOT SUPPORTED as a directional claim** — LLM-DAG wins only 56% of seeds (statistically indistinguishable from chance); recommend removing the ARI-based directional claim rather than reporting the single point estimate. | **Yes** — the ARI-based claim ("LLM-DAG's community structure better matches the raw literature, per ARI") is not supported and should be removed or heavily qualified |
| 14 | Figure 2 provenance | Figure 2 caption/methods | Presented as built from the final, corrected LLM-DAG clustering | `docs/provenance/figure2_vosviewer_rebuild.md` | REPRODUCIBILITY CORRECTION + NEW RESULT NEEDED | The existing Figure 2 (PNG/PDF) was built from the stale, pre-fix-run harmonisation map. Corrected data inputs have been regenerated (Task 11); the figure itself has not yet been rebuilt — a manual VOSviewer step remains (documented, not executed). Do not present the existing Figure 2 as final until it is rebuilt from the corrected inputs. | Possibly — the exact node layout/highlighted examples in Figure 2 may change, though the headline vocab/Q numbers below each panel should use the corrected values already verified |
| 15 | Binary denominator (N=124, 43 gold matches) | §3.1 Results | Headline F1/precision/recall reported without stating the effective binary denominator | `results/test_predictions.csv`, independently re-verified twice | NEW RESULT NEEDED / CLAIM NARROWING | State explicitly: test set n=149; 25 gold-uncertain excluded; primary binary denominator N=124 (43 gold match, 81 gold non-match, TP=41/FP=1/FN=2/TN=80). 1 flipped decision = 0.81 percentage points; 3 flipped = 2.42 points. This directly quantifies the editor's "three errors" concern and should be stated, not left implicit. | No — doesn't change the reported numbers, but materially changes how confidently they should be presented |
| 16 | Uncertainty intervals | §3.1/§3.3 Results (not currently present) | No confidence intervals or seed-sensitivity ranges reported anywhere | Confirmed zero significance-testing/CI code anywhere in the codebase (Phase 0A); Phase 0B added a diagnostic bootstrap CI for the pairwise metrics and a full seed-sensitivity distribution for the downstream metrics | NEW RESULT NEEDED | Add, at minimum: the diagnostic bootstrap 95% CI for precision/recall/F1 (Precision [0.921, 1.000], Recall [0.881, 1.000], F1 [0.917, 1.000] — pair-level resampling, N=10,000, diagnostic only) and the Table 9 seed-sensitivity ranges (#13). | No — additive, doesn't change point estimates |
| 17 | AI manuscript-preparation disclosure | Acknowledgements / Methods (not currently present) | No disclosure of AI-assisted repair/analysis tooling anywhere in the manuscript proxy documents | This repair itself, and the original pipeline's LLM component, are both relevant | NEW RESULT NEEDED | Add a disclosure statement covering: (a) the LLM-DAG pipeline's use of an LLM as the core verification method (already implicit in the method, but should be explicit about tooling), and (b) that this 2026-08 repair — evidence auditing, determinism root-causing, test-suite authoring, documentation correction — was performed with AI-assisted tooling (Claude Code), under human review and approval at each phase. Exact wording is an authorial decision, not made here. | No |

## Notes on scope

- Items 3, 9, 13 involve real numeric/statistical corrections; the rest are
  method-description, wording, or transparency corrections that do not
  change any previously-reported point estimate (with the important
  exception of the ARI-based directional claim in item 13, which should be
  withdrawn as a headline finding).
- This matrix does not draft final manuscript prose. A subsequent pass
  (outside this repair) should produce literal Replace:/With: text against
  the actual manuscript file, using the "Corrected factual content" column
  above as source material.
- Items 10 and 11 both require author input this repair cannot supply —
  see Phase 0A's "Questions for the authors/annotators" and this document's
  109-term investigation for what specifically needs answering.
- **Update, Phase 1A (2026-08-24):** item 10 (39 annotator-hours) is
  resolved by the corresponding author's attestation
  (`docs/provenance/annotation_author_attestation_2026-08-24.md`) —
  reclassify from UNSOURCED ESTIMATE to DOCUMENTED ESTIMATE
  (author-attested, not independently measured). The pilot-size question
  (50 vs. 52) is resolved: 52 is correct and intended; the "50" figures in
  planning documents are acknowledged-obsolete targets, not a live
  discrepancy.

---

# Phase 1A Manuscript Impact Matrix (Update)

**This section extends, and does not replace, the Phase 0B matrix above.**
No manuscript prose is drafted here. Every row below reflects new evidence
generated during Phase 1A (bootstrap CIs, exact error-sensitivity bounds,
per-stratum analysis, three-way class-level metrics, and the rerun-
stability/second-model *plans*, none of which have been executed yet).

| Section | Existing claim | Current verified evidence | Additional Phase 1 evidence planned | Disposition |
|---|---|---|---|---|
| Abstract | States headline F1/precision/recall/coverage as point estimates | Independently re-verified three times now (Phase 0A, 0B, 1A) — numbers are correct | Bootstrap 95% CIs now exist (`results/current_paper/bootstrap_uncertainty_per_method.csv`) | **NARROW** — add a compact CI alongside the point estimate (e.g. "F1 = 0.965, 95% CI [0.917, 1.000]") rather than a bare point estimate |
| RQ2 (model generalisability / robustness, if present in the current RQ framing) | Not currently addressed — only one model was ever run | None yet — second model not executed | Full second-model protocol designed (Task 8), not yet executed; cross-model plan pre-specified (Task 9) | **ADD NEW RESULT** — pending Phase 1B execution; cannot be answered from Phase 1A alone |
| Methods — evaluation section | Describes binary and three-way evaluation; does not currently state the effective binary denominator (N=124) explicitly | Confirmed N=124 (43 gold match, 81 gold non-match) via three independent recomputations | Exact error-sensitivity bounds now computed (`results/current_paper/error_sensitivity_analysis.json`) | **ADD NEW RESULT** — state the denominator explicitly and report the exact 1/2/3-error sensitivity range (F1 swings from 0.9268 to 1.0000 across k=1..3) directly addressing the editor's comment |
| Main benchmark results (Table 6) | Point estimates only, no CIs, no per-stratum breakdown | All point estimates re-verified exactly | Full per-method bootstrap CIs (Task 2) and per-stratum performance (Task 4) now exist | **ADD NEW RESULT** (CIs into the main table; per-stratum table to supplement — see `docs/provenance/current_paper_result_table_plan.md`) |
| Ablation section | A3 framed as an independently-run "no context" ablation | Confirmed (Phase 0B) A3 is a verbatim copy of the full-pipeline row — context was never added to begin with | No new Phase 1A evidence; carried forward from Phase 0B | **REPLACE** wording — state plainly that A3 demonstrates auxiliary context was never used in v1, not that removing it "had no effect" |
| Downstream section (Table 9-equivalent) | Point-estimate Q/ARI/AMI; implies LLM-DAG's community structure is better aligned with the raw literature on both ARI and AMI | Phase 0B: modularity and AMI rankings robust across 50 Louvain seeds (100%/84%); **ARI ranking not supported (56%, chance-level)** | No new Phase 1A evidence; carried forward from Phase 0B, restated here for completeness in the same matrix as the new benchmark-level findings | **REMOVE** the ARI-based directional claim; **NARROW** the AMI/Q claims to report seed medians with IQR, not single point estimates |
| Reproducibility section | Describes the pipeline as deterministic and auditable; historically silent on rerun-stability of the LLM component itself | Phase 0B: downstream Louvain layer's determinism bug found, root-caused, and fixed (20/20 hash-seed verification) | Phase 1A: a complete, dry-run-validated rerun-stability harness now exists for the *LLM* component specifically (Task 6) — not yet executed | **ADD NEW RESULT**, pending Phase 1B execution — until then, state explicitly that formal LLM rerun-stability testing is designed and pending, not yet reported, rather than implying it already exists |
| Limitations | Should discuss single-model, single-annotation-team, small-test-set concerns per the original Scientometrics review | Annotation-team composition now confirmed via author attestation (all roles filled by author-team members) — this is itself a limitation worth stating explicitly, not concealing | Exact quantified error-sensitivity bounds (Task 3) give a precise, citable number for the "small test set" limitation instead of a vague statement | **ADD NEW RESULT** / **NARROW** — state the annotator-independence limitation plainly (the annotators were author-team members, per the attestation) alongside the quantified error-sensitivity bound |
| Conclusion | Broad claim that the LLM-DAG approach outperforms baselines and produces a cleaner thematic structure | F1/precision/recall superiority over baselines: well-supported (bootstrap CI excludes zero for the key comparisons — Task 2). "Cleaner thematic structure": **partially** supported (Q, AMI: yes; ARI: no) | None further planned beyond what Phase 0B/1A have already produced | **NARROW** — the vocabulary-preservation and modularity claims stand; the ARI-based framing should not appear in the conclusion |

## Cross-reference

Full evidence for every "Additional Phase 1 evidence planned" cell above:
`PHASE_1A_PRE_API_REPORT.md` (Task 13) and the individual
`docs/provenance/*.md` files it indexes.

---

# Phase 1B Manuscript Impact Matrix (Update)

**This section extends, and does not replace, the Phase 0B and Phase 1A
matrices above.** Every row reflects REAL evidence from the paid Phase 1B
execution (5 fresh Claude Haiku 4.5 reruns + a real OpenAI
`gpt-5.4-nano-2026-03-17` dev+test run), not a plan or a dry-run. No
manuscript prose is drafted here. Full evidence: `PHASE_1B_RESULTS_REPORT.md`.

| # | Section | Existing claim / Phase 1A status | Real Phase 1B evidence | Disposition |
|---|---|---|---|---|
| 1 | Reproducibility section | Phase 1A: a rerun-stability harness existed but was never executed; manuscript is silent on LLM-level rerun stability | 5 real reruns + historical: Fleiss' kappa=0.9929 (VERY HIGH), only 1/149 pairs (0.67%) ever changed label, precision/recall/F1 bit-identical across all 6 conditions | **ADD NEW RESULT** — state plainly that the primary model's rerun stability was empirically measured (not assumed) and found very high, with the exact kappa and the one unstable pair's nature (a near-threshold confidence wobble, not a substantive match/non-match flip) |
| 2 | RQ2 / model-generalisability (editor's "two or more models" condition) | Phase 1A: no second model executed; disposition was "ADD NEW RESULT — pending Phase 1B" | Real OpenAI `gpt-5.4-nano-2026-03-17` result: F1=0.8916 (vs Claude's 0.9647), Cohen's kappa=0.6852 (81.21% exact agreement), paired bootstrap F1 diff excludes zero | **ADD NEW RESULT, with explicit qualification** — the editor's condition is satisfied (a second, independent-family model was run using the same task, benchmark, and decision policy, with an independently development-tuned threshold and provider-appropriate parameters — NOT an identical or untuned protocol; see reconciliation audit item 6), but the result does NOT show strong cross-model robustness. State this honestly: the *primary* result stands independently verified; the *cross-model robustness* claim is only partially supported. If a qualitative label is attached to the kappa value, mark it explicitly as a conventional interpretation, not part of the measurement (reconciliation audit item 9). |
| 3 | Any framing that could imply "the workflow performs equally well regardless of model" | Not an explicit current claim, but a plausible implicit reading of "we validate with two models" | Real gap: F1 diff -0.073 and recall diff -0.093 (both 95% CI exclude zero); precision diff -0.051 (95% CI includes zero, i.e. statistically unresolved, not evidence of equal precision) | **PREEMPT** — do not let the manuscript imply model-interchangeability. State conservatively that GPT-5.4 nano abstains far more often (13/124 vs. Claude's 0/124 on the gold-binary subset) and this is associated with its recall/F1 deficit; do NOT state the gap is "not precision" — the precision difference is statistically unresolved with this sample, not established as zero (reconciliation audit item 5) |
| 4 | Confidence / calibration discussion (if any) | Not currently discussed at the multi-model level | Both models show discretized, clustered self-reported confidence (Claude: 9 distinct values/149 calls; OpenAI: 13/149); OpenAI has a far larger low-confidence tail (21.5% <0.5 vs Claude's 6.7%) | **ADD NEW RESULT / GUARD** — if confidence is discussed, explicitly disclaim that raw confidence values are not directly comparable across providers (a 0.8 from one model is not the same quantity as a 0.8 from another); report the qualitative distributional difference, not a value-for-value comparison |
| 5 | Three-way (match/non_match/uncertain) evaluation, cross-model | Only reported for the primary model | OpenAI three-way: accuracy=0.8725, macro F1=0.8486, **uncertain-class precision only 0.6389** (many predicted-uncertain pairs are not actually gold-uncertain) | **ADD NEW RESULT** — the uncertain-class miscalibration is a genuine, specific second-model weakness worth naming precisely, not folding into an aggregate number |
| 6 | Per-stratum performance, cross-model | Only reported for the primary model (Phase 1A) | OpenAI stratum `vi` (n=22, mostly gold non-match): P=R=F1=0.0 (3 false-positive "match" calls, 0 true positives) — a clear, small-sample, stratum-specific failure; stratum `iii` shows recall=0.4286 | **ADD NEW RESULT, WITH SMALL-SAMPLE CAVEAT** — name the specific weak stratum if per-stratum cross-model results are reported; do not average it away into a single macro number without disclosure |
| 7 | Error-sensitivity / small-test-set limitation (Phase 1A item 8 in that section) | Phase 1A: quantified 1/2/3-error sensitivity bounds for the primary model only (1 changed decision: F1≈0.952-0.977; 2: ≈0.940-0.989; 3: ≈0.927-1.000) | **Reconciliation audit correction (item 4):** these bounds show a real, not-yet-resolved sensitivity — a 3-decision perturbation swings F1 across a range (~0.073) comparable to the cross-model F1 gap this report treats as credible elsewhere. Phase 1B's real rerun-stability finding (Fleiss' kappa=0.9929) shows the *pinned model itself* does not spontaneously produce such a perturbation across 5 real reruns — a narrower, genuinely supported claim, NOT equivalent to "the primary result is robust to test-set size." The two are different questions. | **DO NOT CLAIM "benchmark-size robustness: SUPPORTED."** State plainly, per the editor's own concern: the benchmark is demonstrably sensitive to a handful of decisions (Phase 1A's own bounds); what Phase 1B adds is that the *model*, run repeatedly, does not itself introduce such perturbations on this benchmark — a reassurance about model stability, not a resolution of small-sample sensitivity. Do not conflate the two in the limitations section. |
| 8 | Cost / reproducibility-cost transparency (if the manuscript discusses API cost at all) | Phase 1A: only the historical $0.0329/149-pair figure existed | Real costs: 5 Claude reruns = $0.1669 total; OpenAI dev+test (500 pairs) = $0.0804; combined $0.2473 | **ADD NEW RESULT** — if cost is discussed, the real multi-model, multi-run cost is now known exactly and can replace any single-run estimate |
| 9 | Conclusion | Phase 1A: F1/precision/recall superiority over baselines well-supported; ARI-based structural claim not supported (unchanged) | Both prior findings stand unchanged and independently re-confirmed by Phase 1B's stability result; the NEW addition is that cross-model generalisability is only partially, not fully, supported | **NARROW / ADD** — the conclusion may now state the primary result is empirically stable across reruns AND across the confusion-matrix perturbations already quantified in Phase 1A, while explicitly scoping the "two models" validation as a robustness check with a disclosed, qualified outcome, not a second confirmation of the same result |
| 10 | Any claim of exact reproducibility for the second-model condition | N/A (no second model existed before Phase 1B) | GPT-5 reasoning models do not support a `seed` parameter (verified, Task 1) — disclosed, not fabricated | **GUARD** — the manuscript must not claim seed-level reproducibility for the OpenAI condition; it may claim it for a future run at `reasoning.effort="none", temperature=0`, understanding OpenAI's own documentation caveats this as not a formal bitwise guarantee |
| 11 | Any "error superset" / "OpenAI's errors are a strict subset relationship" framing (reconciliation audit, item 3) | Not yet in any manuscript draft; flagged pre-emptively | Real three-way contingency (all 149 pairs): 12 pairs exist where OpenAI is correct and Claude is wrong (mostly gold-`uncertain` pairs Claude forces a decision on); the superset claim holds only for the binary decided-pairs framing (N=124) | **PREEMPT** — if error overlap is discussed, state which framing (binary-decided vs. full three-way) any overlap claim uses; a superset claim that is silently binary-scoped would misrepresent the full three-way picture, where the relationship does not hold |
| 12 | Data-availability / reproducibility statement, repository link | Not yet drafted for Phase 1B content | A restricted-data handling issue was found and partially remediated on this branch (verbatim Scopus-derived keyword-pair text committed in two documentation files, since redacted from current content but still present in local git history; not pushed) — see `docs/provenance/phase1b_final_reconciliation_audit.md` item 10. **Additionally, and more urgently:** the same audit incidentally discovered that `results/error_analysis.csv`, `results/test_predictions.csv`, and `results/test_predictions_baselines.csv` — containing raw keyword strings and, for the first, full LLM justification text — are present on `origin/main` and were never part of Phase 0B's existing remediation inventory (item 10, "Additional finding"). | **GUARD, repository readiness (not manuscript prose)** — this branch must not be pushed, merged, or linked from a submission until the author (a) decides on remediation for the local-only history leak, (b) executes the pre-existing Phase 0B harmonisation-map remediation, and (c) addresses the newly-found, apparently-already-public exposure of the three files above — the most urgent of the three. None is a manuscript-text issue, but all three gate repository/submission readiness and must not be treated as resolved by Phase 1B's computational completeness |

## Cross-reference

Full evidence for every row above: `PHASE_1B_RESULTS_REPORT.md`,
`docs/provenance/phase1b_final_reconciliation_audit.md`,
`results/current_paper/phase1b/*.json` and `*.csv`,
`results/current_paper/rerun_stability/real_phase1b_stability_report.json`,
`docs/provenance/openai_gpt54_nano_verification_2026-08-24.md`, and
`docs/provenance/cross_provider_parameter_mapping.md`.
