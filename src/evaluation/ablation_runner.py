"""
Ablation study orchestration.

Runs the full pipeline and all baselines under a configurable
ablation grid (defined in configs/eval_config.yaml), collects
metrics, and writes comparison tables to outputs/tables/.
"""
